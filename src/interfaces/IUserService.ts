export interface IUserService {
  // createUser(user: IUser): Promise<IUser>;
  // getUser(id: string): Promise<IUser | null>;
  
//   updateUser(user: IUser, options: UpdateOptions): Promise<IUser>;
//   deleteUser(id: string): Promise<IUser>;
}
