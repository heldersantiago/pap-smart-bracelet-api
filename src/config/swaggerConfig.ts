// import swaggerJsdoc from 'swagger-jsdoc';
// import yaml from 'yamljs';

// const swaggerOptions = {
//   swaggerDefinition: {
//     info: {
//       title: 'API CRUD OPERATIONS',
//       description: 'This is a basic API that handles all crud operations (Create, read, update, delete)',
//       version: '1.0.0',
//     },
//   },
//   apis: ['../routes/user.ts'], // Specify the path to your route files
// };

// const swaggerDocs = swaggerJsdoc(swaggerOptions);
// const swaggerYaml = yaml.stringify(swaggerDocs);

// export { swaggerDocs, swaggerYaml };
